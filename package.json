{
  "name": "viro",
  "private": true,
  "version": "0.1.0",
  "description": "X + TikTok + WhatsApp + Binance P2P on P2P",
  "license": "MIT",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "clean": "turbo run clean",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.3",
    "turbo": "^2.1.3"
  },
  "engines": {
    "node": ">=20"
  }
}
